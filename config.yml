configurations:
- name: planck-default
  require_monitors:
  - alias: int
    output: INT-1
  - alias: never
    output: NEVER
- name: lorentz-home-4k
  require_monitors:
  - alias: m0
    match: width = 1920 and height = 1080
  - alias: m1
    match: width = 3840 and height = 2160
  apply_commands:
  - 'echo hello from display-config - output of m0 is \"$m0_output\"'
  - 'echo hello from display-config - output of m1 is \"$m1_output\"'


  # - if: $m0.width > 1920
  #   run:
  #   - echo hi

    # - width = 1920 and height = 1080
    # - preferred_mode = 1920x1080:60
    # - not modes has 1920x1080
